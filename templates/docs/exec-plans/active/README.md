# Active Exec Plans

Harness runs in progress live here.

Each run should use:

```text
<YYYY-MM-DD-slug>/
  meta.json
  spec.md
  sprints/
```

Do not archive a run manually unless the harness completion phase has finished and `meta.json` is marked `completed`.
